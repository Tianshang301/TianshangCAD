version: "3.8"

services:
  tianshangcad-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: tianshangcad-server:0.5.0
    ports:
      - "8081:8081"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - TIANGSHANGCAD_RUNTIME=analytic
      - TIANGSHANGCAD_HEADLESS=true
      - TIANGSHANGCAD_TEMP_DIR=/tmp/tianshangcad
      - TIANGSHANGCAD_LOG_LEVEL=INFO
      - TIANGSHANGCAD_API_KEY=${TIANGSHANGCAD_API_KEY:-}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8081/health')"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3
