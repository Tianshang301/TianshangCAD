"""CAD CLI + MCP Server system.

A modern CAD command-line interface and MCP (Model Context Protocol)
server built on top of a pluggable geometry kernel (analytic / OCC /
FreeCAD). Geometry, scenes and parameters are defined via JSON Schema.
"""

__version__ = "0.9.1"
